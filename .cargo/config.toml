# rust-lld ships with the toolchain and links this project measurably faster
# than the default MSVC link.exe (~6.4s -> ~5.9s on an incremental rebuild).
[target.x86_64-pc-windows-msvc]
linker = "rust-lld.exe"
